import React, { useState } from 'react'


export const Loading = (props) => {
        if(props.visible){
            return (
                <div>
                    <h1>LOADING...</h1>
                </div>
                );
            }
        else{
            return
        }
}
